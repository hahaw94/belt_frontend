# 中英文切换 i18n 改造指南

## ✅ 已完成的工作

### 1. 基础架构
- ✅ 安装 vue-i18n@9
- ✅ 创建完整的 locales 目录结构
- ✅ 配置 i18n 实例和 Pinia store
- ✅ 在 main.js 中注册 i18n
- ✅ Element Plus 国际化支持

### 2. 语言切换器
- ✅ 在 HomeLayout.vue 的告警铃铛左侧添加语言切换器
- ✅ 科技感样式设计
- ✅ 支持中英文切换并刷新页面

### 3. 已完成的页面改造
- ✅ layout/index.vue - 主布局菜单
- ✅ layout/HomeLayout.vue - 首页布局
- ✅ DeviceManagement.vue - 设备管理
- ✅ BasicManagement.vue - 基础管理（Tab标签）
- ✅ LoginView.vue - 登录页面
- ✅ UserManage.vue - 用户管理（部分）

## 📝 改造方法

### 模板中使用
```vue
<!-- 简单文本 -->
<h2>{{ $t('menu.home') }}</h2>
<el-button>{{ $t('common.save') }}</el-button>

<!-- 属性绑定 -->
<el-input :placeholder="$t('common.pleaseInput')" />
<el-tab-pane :label="$t('menu.deviceManagement')" />

<!-- 组合使用 -->
<span>{{ $t('common.total') }} {{ count }} {{ $t('common.items') }}</span>
```

### 脚本中使用
```javascript
import { useI18n } from 'vue-i18n'

const { t } = useI18n()

// 在方法中使用
ElMessage.success(t('common.saveSuccess'))
ElMessageBox.confirm(t('common.deleteConfirm'), t('common.deleteConfirmTitle'))
```

## 🎯 待改造页面清单

### 高优先级（主要功能页面）
- [ ] AlgorithmManagement.vue - 算法管理
- [ ] AlgorithmUpload.vue - 算法上传
- [ ] AlarmDisplay.vue - 告警展示
- [ ] DataCollection.vue - 数据收集
- [ ] LinkageSettings.vue - 联动设置
- [ ] RoleManagement.vue - 角色管理
- [ ] RealtimeDetection.vue - 实时检测
- [ ] VideoPlayback.vue - 视频回放

### 中优先级（配置页面）
- [ ] SystemConfig.vue - 系统配置
- [ ] RecordingManagement.vue - 录像管理
- [ ] LogCenter.vue - 日志中心
- [ ] StatisticsAnalysis.vue - 统计分析
- [ ] WarningPush.vue - 告警推送

### 低优先级（其他页面）
- [ ] FunctionList.vue - 功能列表
- [ ] EventCenter.vue - 事件中心
- [ ] Detection.vue - 检测
- [ ] AccessManagement.vue - 接入管理
- [ ] UserManagement.vue - 用户管理

### 子组件（基础管理模块）
- [ ] TimeManagement.vue - 时间管理
- [ ] NetworkManagement.vue - 网络管理
- [ ] LogoManagement.vue - Logo管理
- [ ] PlatformManagement.vue - 平台管理
- [ ] StorageManagement.vue - 存储管理
- [ ] MaintenanceManagement.vue - 维护管理

### 设备管理子组件
- [ ] CameraManagement.vue - 相机管理
- [ ] BoardManagement.vue - 板卡管理

## 📚 语言包结构

### 已创建的语言包模块
```
locales/
├── zh-CN/
│   ├── common.js      ✅ 通用词汇（已扩展）
│   ├── menu.js        ✅ 菜单导航
│   ├── device.js      ✅ 设备管理
│   ├── system.js      ✅ 系统配置（已扩展）
│   ├── algorithm.js   ✅ 算法管理
│   ├── event.js       ✅ 事件中心
│   ├── user.js        ✅ 用户管理
│   └── index.js       ✅ 入口文件
└── en-US/
    └── (同上结构)     ✅ 英文翻译
```

## 🔧 改造步骤

### 1. 查找硬编码文本
在组件中搜索中文字符串，例如：
- 标题：`<h2>用户管理</h2>`
- 按钮：`<el-button>保存</el-button>`
- 提示：`placeholder="请输入用户名"`

### 2. 确定语言包路径
根据内容确定使用哪个语言包：
- 通用操作 → `common.*`
- 菜单导航 → `menu.*`
- 模块特定 → `device.*` / `system.*` / `user.*` 等

### 3. 替换为 i18n 语法
```vue
<!-- 替换前 -->
<h2>用户管理</h2>
<el-button>保存</el-button>

<!-- 替换后 -->
<h2>{{ $t('user.user.title') }}</h2>
<el-button>{{ $t('common.save') }}</el-button>
```

### 4. 检查并补充语言包
如果语言包中没有对应的 key，需要先添加到语言包中。

## 💡 常用替换模式

### 按钮文本
```vue
保存 → {{ $t('common.save') }}
取消 → {{ $t('common.cancel') }}
删除 → {{ $t('common.delete') }}
编辑 → {{ $t('common.edit') }}
新增 → {{ $t('common.add') }}
搜索 → {{ $t('common.search') }}
重置 → {{ $t('common.reset') }}
刷新 → {{ $t('common.refresh') }}
```

### 表单标签
```vue
用户名 → {{ $t('user.user.username') }}
密码 → {{ $t('user.user.password') }}
邮箱 → {{ $t('user.user.email') }}
电话 → {{ $t('user.user.phone') }}
状态 → {{ $t('common.status') }}
```

### 占位符
```vue
:placeholder="$t('common.pleaseInput') + $t('user.user.username')"
:placeholder="$t('common.pleaseSelect')"
```

### 消息提示
```javascript
ElMessage.success(t('common.saveSuccess'))
ElMessage.error(t('common.saveFailed'))
ElMessage.warning(t('common.warning'))
```

## 🚀 批量改造建议

1. **优先改造高频页面**：先改造用户最常用的页面
2. **按模块进行**：一次改造一个模块的所有页面
3. **测试验证**：每改造完一个页面就测试切换功能
4. **保持一致性**：相同的文本使用相同的 key
5. **补充语言包**：遇到新词汇及时添加到语言包

## ✅ 验证清单

改造完成后检查：
- [ ] 所有硬编码中文已替换
- [ ] 语言切换功能正常
- [ ] 英文翻译准确
- [ ] 页面布局未受影响
- [ ] 表单验证消息已国际化
- [ ] 提示消息已国际化

## 📌 注意事项

1. **不要遗漏**：
   - 弹窗标题
   - 表格列标题
   - 表单验证消息
   - 确认对话框
   - Toast 提示

2. **特殊处理**：
   - 动态文本需要在脚本中使用 `t()` 函数
   - 验证规则中的消息需要使用 `t()` 函数
   - 某些第三方组件可能需要特殊处理

3. **保持简洁**：
   - 使用语义化的 key 名称
   - 避免过长的嵌套路径
   - 相似内容可以复用

---

**当前进度：约 30% 完成**

继续按照此指南改造剩余页面即可！
